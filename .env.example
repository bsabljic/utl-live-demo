# Claude (optional; if absent, summaries fall back to mock)
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxx

# CSV source (Vercel Blob public URL or GitHub raw)
CSV_PUBLIC_URL=https://blob.vercel-storage.com/utl-metrics.csv

# KV (for shared rate-limit; auto-created in Vercel -> Storage -> KV)
KV_URL=kv://your-kv-url

# Slack (optional) for deploy webhook or critical alerts
SLACK_WEBHOOK=https://hooks.slack.com/services/XXX/YYY/ZZZ

# Deploy webhook secret (GitHub Action -> POST /api/deploy-webhook)
DEPLOY_SECRET=super-secret-deploy-token
